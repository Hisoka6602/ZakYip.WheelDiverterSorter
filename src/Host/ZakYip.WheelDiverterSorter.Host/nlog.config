<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Info"
      internalLogFile="logs/internal-nlog.txt">

  <!-- enable asp.net core layout renderers -->
  <extensions>
    <add assembly="NLog.Web.AspNetCore"/>
  </extensions>

  <!-- the targets to write to -->
  <targets>
    <!-- write logs to file -->
    <target xsi:type="File" name="allfile" fileName="logs/all-${shortdate}.log"
            layout="${longdate}|${event-properties:item=EventId:whenEmpty=0}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}" />

    <!-- write error and fatal logs to separate error file -->
    <target xsi:type="File" name="errorfile" fileName="logs/error-${shortdate}.log"
            layout="${longdate}|${event-properties:item=EventId:whenEmpty=0}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring,Data:MaxInnerExceptionLevel=10}"
            archiveFileName="logs/archives/error-{#}.log"
            archiveEvery="Day"
            archiveNumbering="DateAndSequence"
            maxArchiveFiles="30" />

    <!-- write driver/hardware communication logs to separate file -->
    <target xsi:type="File" name="driverfile" fileName="logs/driver-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            archiveFileName="logs/archives/driver-{#}.log"
            archiveEvery="Day"
            archiveNumbering="DateAndSequence"
            maxArchiveFiles="14" />

    <!-- write parcel lifecycle logs to a separate daily rolling file -->
    <target xsi:type="File" name="parcellifecycle" fileName="logs/parcel-lifecycle-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${message}" 
            archiveFileName="logs/archives/parcel-lifecycle-{#}.log"
            archiveEvery="Day"
            archiveNumbering="Rolling"
            maxArchiveFiles="30" />

    <!-- write parcel trace logs to a separate daily rolling file with JSON format -->
    <target xsi:type="File" name="parceltrace" fileName="logs/trace/parcel-trace-${shortdate}.log"
            layout="${message}"
            archiveFileName="logs/archives/trace/parcel-trace-{#}.log"
            archiveEvery="Day"
            archiveNumbering="DateAndSequence"
            maxArchiveFiles="14" />

    <!-- write communication logs to separate file -->
    <target xsi:type="File" name="communicationfile" fileName="logs/communication-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger}|${message} ${exception:format=tostring}"
            archiveFileName="logs/archives/communication-{#}.log"
            archiveEvery="Day"
            archiveNumbering="DateAndSequence"
            maxArchiveFiles="14" />

    <!-- write to the void -->
    <target xsi:type="Null" name="blackhole" />
  </targets>

  <!-- rules to map from logger name to target -->
  <rules>
    <!-- Parcel Trace logs go to dedicated trace file -->
    <logger name="ZakYip.WheelDiverterSorter.Observability.Tracing.FileBasedParcelTraceSink" minlevel="Trace" writeTo="parceltrace" final="true" />
    
    <!-- Parcel Lifecycle logs go to separate file -->
    <logger name="ZakYip.WheelDiverterSorter.Observability.ParcelLifecycleLogger" minlevel="Trace" writeTo="parcellifecycle" final="true" />

    <!-- Driver/hardware logs go to driver file (ShuDiNiao, Modi, Leadshine, etc.) -->
    <logger name="ZakYip.WheelDiverterSorter.Drivers.*" minlevel="Debug" writeTo="driverfile" />
    
    <!-- Communication logs go to communication file -->
    <logger name="ZakYip.WheelDiverterSorter.Communication.*" minlevel="Debug" writeTo="communicationfile" />

    <!-- Skip non-critical Microsoft logs and so on -->
    <logger name="Microsoft.*" maxlevel="Info" final="true" />
    <logger name="System.Net.Http.*" maxlevel="Info" final="true" />

    <!-- Error and Fatal logs go to error file (all namespaces) -->
    <logger name="*" minlevel="Error" writeTo="errorfile" />

    <!-- All other logs -->
    <logger name="*" minlevel="Debug" writeTo="allfile" />
  </rules>
</nlog>
