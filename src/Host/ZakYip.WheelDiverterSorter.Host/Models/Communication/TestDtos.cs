namespace ZakYip.WheelDiverterSorter.Host.Models.Communication;

// Stub DTOs for compatibility (test endpoints removed for performance)
public record ConnectionTestResponse { }
public record TestParcelRequest { }
public record TestParcelResponse { }

// Stub DTOs for compatibility (performance test endpoints removed)
public record IoPerformanceTestRequest { }
public record IoPerformanceTestResponse { }
